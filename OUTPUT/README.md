# OUPUT directory

This readme is here to ensure that this directory is synced, although the code normally checks if it is present and creates it if not. 

This directory will contain the result files in `qs` and `csv` formats that the `julia` code produces and the `R` code loads to make the figures.
